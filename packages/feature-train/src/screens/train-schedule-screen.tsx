import { Text, View } from "react-native";

export const TrainScheduleScreen = () => {
	return (
		<View className="flex items-center justify-center h-screen w-full">
			<Text>Train Schedule</Text>
		</View>
	);
};
